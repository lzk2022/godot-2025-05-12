你是一个C++高手，帮我使用中文注释代码，函数注释和参数在同一行，放在函数上面，类的作用也需要注释，不要添加类名，字符串不需要翻译,宏内部不需要注释，重载操作符函数不需要注释,原样输出，函数不要在右边注释，只输出注释后的代码，不需要其他信息。注释格式如下
int i = 0; // 变量
// 加载资源包文件:资源包路径、是否替换已存在的文件（默认为 true）、加载偏移量（默认为 0）
bool _load_resource_pack(const String &p_pack, bool p_replace_files = true, int p_offset = 0);